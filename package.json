{
  "name": "effect-tasks",
  "private": true,
  "sideEffects": false,
  "type": "module",
  "workspaces": ["packages/*"],
  "scripts": {
    "docs:dev": "vocs dev",
    "docs:build": "vocs build",
    "docs:preview": "vocs preview",
    "worker:run": "cd packages/worker && bun run start"
  },
  "dependencies": {},
  "devDependencies": {},
  "engines": {
    "node": ">=20.0.0"
  }
}