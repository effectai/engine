{
  "name": "effect-tasks",
  "private": true,
  "sideEffects": false,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "docs:preview": "vocs preview",
    "worker:run": "cd packages/worker && bun run start"
  },
  "devDependencies": {
    "vitest": "^2.1.3"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}