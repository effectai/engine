{
  "name": "effect-tasks",
  "private": true,
  "sideEffects": false,
  "packageManager": "pnpm@8.15.6",
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "dev": "pnpm turbo dev",
    "build": "pnpm turbo build",
    "docs:dev": "pnpm --filter @effectai/docs run dev",
    "test": "vitest"
  },
  "devDependencies": {
    "vitest": "^2.1.3",
    "turbo": "2.2.3"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}