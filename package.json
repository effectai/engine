{
  "name": "effect-tasks",
  "private": true,
  "sideEffects": false,
  "type": "module",
  "scripts": {
    "docs:dev": "pnpm --filter @effectai/docs run dev",
    "test": "vitest"
  },
  "devDependencies": {
    "vitest": "^2.1.3"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}