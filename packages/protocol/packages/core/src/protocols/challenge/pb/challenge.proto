syntax = "proto3";

import "../../task/pb/task.proto";

message Challenge {
  string id = 1;
  string created_at = 2;
  Task task = 3;
//the answer of the challenge that has to match with the result string of the task.
  string answer = 4;
}
