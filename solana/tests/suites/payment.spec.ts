import { describe, it } from "vitest";

describe("Payment Program", async () => {
	it("can create a payment pool", () => {
		console.log("test");
	});

	it("can claim a payment", () => {
		console.log("test");
	});
});
